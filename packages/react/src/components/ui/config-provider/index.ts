import ConfigProvider, { ConfigConsumer, useConfig, defaultConfig } from './config-provider';

export type { Config } from './config-provider';

export { ConfigProvider, ConfigConsumer, useConfig, defaultConfig };

export default ConfigProvider;
