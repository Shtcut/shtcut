import { MessageComponent } from '@shtcut/components/ui/social-component';
import React from 'react';

const MessageContainer = () => {
    return <MessageComponent />;
};

export default MessageContainer;
