import { Dict } from '@shtcut-ui/react';
import { UpdatePasswordForm } from '@shtcut/components/form';

export const UpdatePasswordContainer = () => {
    const handleUpdatePasswordSubmit = (payload: Dict) => {};

    return <UpdatePasswordForm />;
};
