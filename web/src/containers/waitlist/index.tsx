import WaitListComponent from '@shtcut/components/ui/waitlist';
import React from 'react';

const WaitListContainer = () => {
    return (
        <div>
            <WaitListComponent />
        </div>
    );
};

export default WaitListContainer;
