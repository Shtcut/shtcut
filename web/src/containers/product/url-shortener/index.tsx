import { ProUrlShortenerComponent } from '@shtcut/components/ui/product';
import React from 'react';

const ProUrlShortenerContainer = () => {
    return <ProUrlShortenerComponent />;
};

export default ProUrlShortenerContainer;
