

export default function CustomLandingLayout(props) {
    return (
        <div className="flex min-h-screen flex-col justify-between">
            {props.children}
            
        </div>
    )   
}