import { WorkspaceContainer } from '@shtcut/containers/auth';
import AuthLayout from '../layout';

const Workspace = () => {
    return (
        <AuthLayout>
            <WorkspaceContainer />
        </AuthLayout>
    );
};

export default Workspace;
