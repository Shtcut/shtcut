'use client';

import { LayoutBody } from '@shtcut/components';
import LinkBiosContainer from '@shtcut/containers/url-shorten-er/link-bios';

const LinkBio = () => {
    return (
        <LayoutBody className="container">
            <LinkBiosContainer />
        </LayoutBody>
    );
};

export default LinkBio;
