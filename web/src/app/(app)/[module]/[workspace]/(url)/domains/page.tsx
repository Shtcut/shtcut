'use client';

import { LayoutBody } from '@shtcut/components';
import DomainsContainer from '@shtcut/containers/domains';

const Domains = () => {
    return (
        <LayoutBody>
            <DomainsContainer />
        </LayoutBody>
    );
};

export default Domains;
