'use client';

import { LayoutBody } from '@shtcut/components';
import { CreateLinkContainer } from '@shtcut/containers';

const CreateLink = () => {
    return (
        <LayoutBody className="">
            <CreateLinkContainer />
        </LayoutBody>
    );
};

export default CreateLink;
