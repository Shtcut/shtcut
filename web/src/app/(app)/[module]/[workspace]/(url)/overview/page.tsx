'use client';
import { LayoutBody } from '@shtcut/components';
import { OverviewContainer } from '@shtcut/containers/dashboard/url-shorten';

const Overview = () => {
    return (
        <LayoutBody className="">
            <OverviewContainer />
        </LayoutBody>
    );
};

export default Overview;
