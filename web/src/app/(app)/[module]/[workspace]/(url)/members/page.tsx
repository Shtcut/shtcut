'use client';
import { LayoutBody } from '@shtcut/components';
import MembersContainer from '@shtcut/containers/members/members-container';

const Members = () => {
    return (
        <LayoutBody className="">
            <MembersContainer />
        </LayoutBody>
    );
};

export default Members;
