import WaitListContainer from '@shtcut/containers/waitlist';
import React from 'react';

const WaitList = () => {
    return (
        <div>
            <WaitListContainer />
        </div>
    );
};

export default WaitList;
