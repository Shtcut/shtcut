import React from 'react';

const CreateWorkSpaceLayout = ({ children }: React.PropsWithChildren) => {
    return (
        <>
            <main className="">{children}</main>
        </>
    );
};

export default CreateWorkSpaceLayout;
